## Bug Fixes


* Added memoization to resizers
* Prevent user selection when resizing
* Using `borderBoxSize` instead of `contentRect` in `ResizeObserver`


## Features

- Added `useDimensions()`
- Added `useMove()``, added constraints
- Added `useResize()`
- Added resize constraints
- Added `Movable.inscribe()`
- Added `Resizable.inscribe()`
- Added missing `useCallback()` dependency
- Removed redundant if statement
- Added number utilities
- Changed `useMove()` API, added operations & transformers
- Changed `angle()` API
- Changed `useResize()` API