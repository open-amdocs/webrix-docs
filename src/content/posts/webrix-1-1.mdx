

## Bug Fixes

* Changed `MIN_THUMB_LENGTH` to pixels instead of percentage, fixed `getThumbLength/getThumbPosition` to work properly with custom track lengths.
* Fixed `Scrollable` positioning algorithm
* Added memoization to resizers
* Removed user-select from resizers
* Added support for touch events on `Movable`

## Features

* Exposing `Scrollable` utils & default scrollbars